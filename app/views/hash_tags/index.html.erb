<h1>Listing hash_tags</h1>

<table>
  <tr>
    <th>Tag</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @hash_tags.each do |hash_tag| %>
  <tr>
    <td><%= hash_tag.tag %></td>
    <td><%= hash_tag.user %></td>
    <td><%= link_to 'Show', hash_tag %></td>
    <td><%= link_to 'Edit', edit_hash_tag_path(hash_tag) %></td>
    <td><%= link_to 'Destroy', hash_tag, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Hash tag', new_hash_tag_path %>
